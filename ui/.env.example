# PDF Extractor Frontend Environment Variables
# Copy this file to .env.local and update the values for your environment

# API Configuration
# Base URL for the PDF Extractor API
VITE_API_BASE_URL=https://your-api-gateway-url.execute-api.region.amazonaws.com/stage

# API Key for authentication with the API Gateway
# Get this from your AWS API Gateway console or deployment output
VITE_API_KEY=your-api-key-here

# Development vs Production
# VITE_API_BASE_URL=http://localhost:8000  # For local development
# VITE_API_KEY=local-dev-key               # For local development

# Notes:
# - All environment variables for Vite must start with VITE_
# - These values will be embedded in the client-side bundle
# - Do not put sensitive secrets here - use API keys only
# - For production, set these in your deployment environment